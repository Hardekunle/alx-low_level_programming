#include "main.h"

/**
 * puts2 - Prints character in even postion.
 * @str: string.
 */
void puts2(char *str)
{
	int len;
	int i;

	len = 0;
	while (val != '\0')
	{
		len++;
		val = *(s + len);
	}

	i = 0;
	while (i < len)
	{
		_putchar(*(str + i));
		i += 2;
	}

	_putchar('\n');
}
~
